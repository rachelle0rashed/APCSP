# All you have to do in this exercise is write a good comment that explains
# what local variables are. You should also give an example of a function
# and what the local variables are, and what the scope is of the variable.

# The local variables are specific type of function that only available within the content of a particular expression and can only be accessed within the function that defines time.
# The scope is the beginning and end block within which is it declared

def sum(x,y):
    result = x + y
    return result
    
x = 50
print(x)

y = 25
print(y)